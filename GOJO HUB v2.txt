function GetGitSoundID(GithubSnd, SoundName)
    local fileName = "basic" .. tostring(SoundName) .. ".mp3"

    if not isfile(fileName) then
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Downloading Sound",
            Text = "Downloading " .. SoundName .. "...",
            Duration = 3
        })
        
        local success, result = pcall(function()
            return game:HttpGet(GithubSnd)
        end)
        
        if success then
            writefile(fileName, result)
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Download Complete",
                Text = SoundName .. " downloaded successfully!",
                Duration = 3
            })
        else
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Download Failed",
                Text = "Failed to download " .. SoundName,
                Duration = 5
            })
            return nil
        end
    end

    return (getcustomasset or getsynasset)(fileName)
end

local old intro = GetGitSoundID("https://github.com/r3k33551-bot/UMTP/raw/refs/heads/main/SpotiDownloader.com%20-%20Basics%20in%20Behavior%20-%20Blue%20Version%20-%20The%20Living%20Tombstone.mp3", "old intro")
local zynx = GetGitSoundID("https://github.com/r3k33551-bot/UMTP/raw/refs/heads/main/SpotiDownloader.com%20-%20Basics%20in%20Behavior%20-%20Blue%20Version%20-%20The%20Living%20Tombstone.mp3", "zynx")--[[name and links change the locaal name if you want]]

--sound

local sound1 = Instance.new("Sound")
sound1.SoundId = zynx --[[change the name to your sound name (the one you put after the sound links]]
sound1.Parent = workspace
sound1.Looped = false
sound1.Name = "DestroyOnUltOff"
sound1.TimePosition = 2
sound1.Volume = 1
sound1:Play()

loadstring(game:HttpGet("https://raw.githubusercontent.com/Onihub-sigma/OniHub-Gojo-Public/refs/heads/main/Gojo%20Onihub%20Public.txt"))()

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "GOJO GUI",
   Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
   LoadingTitle = "IDK",
   LoadingSubtitle = "by MIYKIO",
   ShowText = "SHIT", -- for mobile users to unhide rayfield, change if you'd like
   Theme = "Default", -- Check https://docs.sirius.menu/rayfield/configuration/themes

   ToggleUIKeybind = "K", -- The keybind to toggle the UI visibility (string like "K" or Enum.KeyCode)

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false, -- Prevents Rayfield from warning when the script has a version mismatch with the interface

   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = "Big Hub"
   },

   Discord = {
      Enabled = false, -- Prompt the user to join your Discord server if their executor supports it
      Invite = "noinvitelink", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ ABCD would be ABCD
      RememberJoins = true -- Set this to false to make them join the discord every time they load it up
   },

   KeySystem = true, -- Set this to true to use our key system
   KeySettings = {
      Title = "Untitled",
      Subtitle = "Key System",
      Note = "No method of obtaining the key is provided", -- Use this to tell the user how to get a key
      FileName = "Key", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
      SaveKey = false, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"AZULYROJORITUALDELVACIOPURPURA"} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
   }
})

local Tab1 = Window:CreateTab("TOOLS & STUFF", 4483362458) -- Title, Image
local Tab2 = Window:CreateTab("MOVESETS", 15124465439) -- Title, Image

local Button = Tab2:CreateButton({
   Name = "MOVESET suiryu",
   Callback = function()
       loadstring(game:HttpGet("https://raw.githubusercontent.com/PlexTheOne/TSB/refs/heads/main/GojoSatoru.lua"))()
   -- The function that takes place when the button is pressed
   end,
})

local Button = Tab2:CreateButton({
   Name = "GOJO V2",
   Callback = function()
   loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-The-Strongest-Battleground-Gojo-manga-Tsb-30660"))()
      -- The function that takes place when the button is pressed
   end,
})

local Button = Tab2:CreateButton({
   Name = "GOJO jjs",
   Callback = function()
   loadstring(game:HttpGet("https://raw.githubusercontent.com/damir512/jjsgojov3/main/SaitamaToGojoV3_SOURCE-obfuscated_2.txt",true))()
      -- The function that takes place when the button is pressed
   end,
})

local Button = Tab1:CreateButton({
   Name = "VFX",
   Callback = function()
       loadstring(game:HttpGet("https://raw.githubusercontent.com/r3k33551-bot/Acortador-de-scripts/refs/heads/main/gojos%20infinity.txt"))()
   -- The function that takes place when the button is pressed
   end,
})

local Button = Tab1:CreateButton({
   Name = "INTRO",
   Callback = function()
       loadstring(game:HttpGet("https://raw.githubusercontent.com/Onihub-sigma/OniHub-Gojo-Public/refs/heads/main/Gojo%20Onihub%20Public.txt"))()
   -- The function that takes place when the button is pressed
   end,
})

local Button = Tab1:CreateButton({
   Name = "RUN",
   Callback = function()
       loadstring(game:HttpGet("https://raw.githubusercontent.com/r3k33551-bot/Acortador-de-scripts/refs/heads/main/run%20tool.txt"))()
   -- The function that takes place when the button is pressed
   end,
})

local Button = Tab1:CreateButton({
   Name = "TP",
   Callback = function()
       loadstring(game:HttpGet("https://raw.githubusercontent.com/r3k33551-bot/Acortador-de-scripts/refs/heads/main/TP"))()
   -- The function that takes place when the button is pressed
   end,
})

Rayfield:Notify({
   Title = "IDK",
   Content = "UNRELEASED LEAK",
   Duration = 6.5,
   Image = 4483362458,
})